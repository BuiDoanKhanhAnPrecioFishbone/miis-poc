# axe-core run — 2026-08-17

| | |
|---|---|
| Tool | axe-core 4.x, run in-page against the dev server |
| Rule set | `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa` |
| Viewport | 1440 × 1000, plus 720 × 500 (equivalent to 200 % zoom at 1440) |
| Build | `main`, after the update plan and the design-system consolidation |

Re-run after the design-system work (`docs/08-design-system-and-interaction-plan.md`
days 1–2) and the AI-colour change, which touched every screen: consolidated buttons, the new `DataTable` with
sortable and sticky headers, the `Callout` feedback component, the compact
confidentiality marker and the `Rationale` annotation layer.

## Result

**No violations on any route tested, in either language, with requirement tags on or
off.** Between 27 and 31 rule checks pass per route.

| Route | Passes | Violations |
|---|---|---|
| `/` (Avtalsadministratör) | 28 | 0 |
| `/registrera` | 27 | 0 |
| `/sok` | 29 | 0 |
| `/rapporter` | 30 | 0 |
| `/medling` (tags on) | 28 | 0 |
| `/medling/M-2027-12` | 28 | 0 |
| `/dokument` | 29 | 0 |
| `/allmanheten` | 31 | 0 |
| `/avtal` (placeholder pattern) | 26 | 0 |
| `/dokument` with the session-timeout modal open | — | 0 |
| `/dokument` at 720 px (200 % zoom) | — | 0 |

## Violations found and fixed across the two runs

All were found by the tool, not by review — which is the argument for running it.

1. **`color-contrast` (serious)** — `text-primary/80` on the mint decision-support panel
   in `/medling/[id]`. Reduced-opacity text was removed everywhere it was used
   (`text-primary/80`, `text-sand-foreground/80`, `text-demo-foreground/80`); the tokens
   are now used at full strength. Opacity is a convenient way to soften text and a
   reliable way to fail 1.4.3.

2. **`scrollable-region-focusable` (serious)** — the protocol pane in `/registrera`
   scrolls, and its content was not focusable, so a keyboard user could not reach the
   lower half of the document. It is now `tabindex="0"` with `role="region"` and the
   file name as its accessible name (2.1.1). The same treatment is built into
   `DataTable`, so every long table inherits it rather than repeating the bug.

## Manual checks

Automated tooling catches roughly a third of WCAG issues. These were checked by hand:

| Check | Result |
|---|---|
| 200 % zoom (720 px effective width) | No horizontal scrolling; `document.scrollWidth === innerWidth` on every route |
| Responsive (NFUI-002) | Sidebar rail at ≥1024 px, narrow rail 768–1023 px, stacked above content below 768 px. No horizontal overflow at 700, 768 or 1440 px |
| Focus return after a modal | Session-timeout dialog returns focus to the control that opened it |
| Keyboard trap (2.1.2) | Tab is cycled inside the dialog; Escape closes it |
| Colour as the only carrier (1.4.1) | FR-012 status uses colour **and** shape (filled circle / filled square / hollow ring) **and** a visible text label. System feedback never reuses those three hues and is distinguished by form: `Callout` has a left rule, an icon and a label word |
| Trend colours | None. Percentages are `text-foreground`; MI does not colour a settlement good or bad |
| AI colour | Violet `--mi-ai-100/500/800`, restored from the CEO's US-01 sketch. 8.47:1 text on badge, 4.78:1 border against paper. Deliberately outside the MI palette so a machine proposal never reads as an MI-registered fact |
| Sortable table headers | `aria-sort` reflects the active column and direction; the sort control is a real button with a visually hidden description |
| Language of page (3.1.1) | `<html lang>` follows the language switch — `sv` by default, `en` when toggled |
| Requirement-tag toggle | Toggling causes zero layout shift: identical `scrollHeight` and identical element positions on `/` and on `/registrera`, the densest screen |
| Annotation layer | `.rationale` is `display: none` with tags off — 24 in-page usages plus 12 from the dashboard read model. `/registrera` is 217 px shorter in the product view |

## Not yet covered

- Testing with a real screen reader (NVDA / VoiceOver). Announcements are marked up
  (`aria-live`, `role="alertdialog"`, `aria-current`, `aria-sort`) but have not been heard.
- Contrast of the sand palette against every background it is used on was checked only
  where axe encountered it, i.e. on rendered pages rather than exhaustively across the
  token matrix.
- Browsers other than Chromium. MI has not yet answered which browsers are in scope.
